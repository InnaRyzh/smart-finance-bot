# 🚀 Инструкция по развертыванию на Railway

## Шаг 1: Подготовка GitHub репозитория

1. Создай новый репозиторий на GitHub
2. Выполни команды в терминале:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/твой-username/smart-finance-bot.git
git branch -M main
git push -u origin main
```

## Шаг 2: Развертывание на Railway

1. Зайди на [railway.app](https://railway.app) и войди через GitHub
2. Нажми **"New Project"**
3. Выбери **"Deploy from GitHub repo"**
4. Выбери репозиторий `smart-finance-bot`
5. Railway автоматически определит Node.js проект и начнет сборку
6. Railway выполнит `npm install` и `npm run build` автоматически

## Шаг 3: Настройка переменных окружения

**КРИТИЧЕСКИ ВАЖНО!**

1. В проекте Railway открой вкладку **"Variables"**
2. Добавь новую переменную:
   - **Key:** `GEMINI_API_KEY`
   - **Value:** твой ключ API от Google Gemini
3. Нажми **"Add"**
4. Railway автоматически пересоберет проект

> 💡 Где взять ключ Gemini: https://aistudio.google.com/app/apikey

## Шаг 4: Получение ссылки

1. После успешной сборки Railway даст тебе ссылку вида:
   ```
   https://smart-finance-bot-production-xxxx.up.railway.app
   ```
2. Скопируй эту ссылку - она понадобится для Telegram

## Шаг 5: Создание Telegram бота

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Отправь команду `/newbot`
3. Введи имя бота (например: `Мой Финансист`)
4. Введи юзернейм (например: `my_super_money_bot`)
5. Сохрани токен (может понадобиться позже)

## Шаг 6: Настройка Web App кнопки

1. В BotFather отправь `/mybots`
2. Выбери своего бота из списка
3. Нажми **Bot Settings** → **Menu Button** → **Configure Menu Button**
4. Выбери **Web App**
5. Вставь ссылку из Railway (Шаг 4)
6. Введи название кнопки: **"Мой Кошелек"** или **"Финансы"**
7. Готово! ✅

## Проверка работы

1. Открой своего бота в Telegram
2. Внизу должна быть кнопка меню с названием, которое ты указал
3. Нажми на кнопку - должно открыться приложение
4. Попробуй добавить транзакцию, например: "Миша 200 долларов"

## Возможные проблемы

### Ошибка при сборке на Railway
- Проверь, что переменная `GEMINI_API_KEY` добавлена в Variables
- Убедись, что ключ валидный и активный

### Бот не открывается
- Проверь, что ссылка Railway правильная и доступна
- Убедись, что проект успешно развернут (зеленый статус)

### API не работает
- Проверь переменную окружения `GEMINI_API_KEY` в Railway
- Убедись, что ключ имеет права на использование Gemini API

