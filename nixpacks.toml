# NIXPACKS конфигурация для Railway
# Гарантирует что переменные окружения доступны во время сборки

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

