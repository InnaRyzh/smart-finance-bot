# 💾 Где хранятся данные

## Хранение данных

Все данные хранятся **локально в браузере** через **localStorage** (встроенное хранилище браузера).

### Что хранится:

1. **Транзакции** (`smart_finance_transactions_v1`)
   - Все доходы и расходы
   - Формат: JSON массив транзакций
   - Хранится в localStorage браузера

2. **Курс USD/UAH** (`smart_finance_usd_rate`)
   - Текущий курс доллара
   - Значение по умолчанию: 41.5

### Где находится:

- **В браузере Telegram** (если открываешь через Web App)
- **В обычном браузере** (если открываешь по прямой ссылке)

### Важно знать:

✅ **Плюсы:**
- Данные хранятся локально - быстрый доступ
- Не нужен интернет для просмотра данных
- Приватность - данные не отправляются на сервер

⚠️ **Ограничения:**
- Данные привязаны к конкретному браузеру/устройству
- Если очистишь данные браузера - данные пропадут
- Если откроешь в другом браузере - данных там не будет
- Нет синхронизации между устройствами

### Как посмотреть данные:

1. Открой DevTools в браузере (F12)
2. Вкладка **Application** (или **Хранилище**)
3. **Local Storage** → выбери домен приложения
4. Там увидишь ключи:
   - `smart_finance_transactions_v1` - все транзакции
   - `smart_finance_usd_rate` - курс доллара

### Резервное копирование:

Если хочешь сохранить данные:
1. Открой DevTools → Application → Local Storage
2. Скопируй значение `smart_finance_transactions_v1`
3. Сохрани в текстовый файл

### Восстановление:

Чтобы восстановить данные:
1. Открой DevTools → Application → Local Storage
2. Вставь сохраненное значение в `smart_finance_transactions_v1`
3. Обнови страницу

### Код хранения:

Файл: `services/storageService.ts`

```typescript
const STORAGE_KEY = 'smart_finance_transactions_v1';
const RATE_KEY = 'smart_finance_usd_rate';

// Данные сохраняются в localStorage браузера
localStorage.setItem(STORAGE_KEY, JSON.stringify(transactions));
```

