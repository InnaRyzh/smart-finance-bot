# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ: API ключ не работает

## Проблема:
API ключ правильный, но приложение все равно показывает ошибку.

## Причина:
Переменные окружения должны быть доступны **ВО ВРЕМЯ СБОРКИ**, а не только при запуске.

## Решение:

### Шаг 1: Убедись что переменная добавлена ПЕРЕД сборкой

1. Открой Railway → **Variables**
2. Проверь что есть:
   - **Key:** `GEMINI_API_KEY`
   - **Value:** `AIzaSyDbS-zqaSQWvAkFmYqfP5ef4gSHHzU9JcE`

### Шаг 2: ПРИНУДИТЕЛЬНАЯ ПЕРЕСБОРКА

1. В Railway открой **Deployments**
2. Нажми **"Redeploy"** или **"Deploy Latest"**
3. Это запустит новую сборку с переменными окружения

### Шаг 3: Проверь логи сборки

1. Во время сборки открой **View Logs**
2. Ищи строки:
   ```
   🔍 Проверка переменных окружения:
     GEMINI_API_KEY из env: ✅ установлен
     Итоговый ключ: ✅ установлен
   ```
3. Если видишь `❌ ПУСТОЙ!` - значит переменная не передается

### Шаг 4: Альтернативное решение (если не помогло)

Если переменные все равно не передаются, можно использовать другой подход:

1. Создай файл `.env.production` в корне проекта
2. Добавь туда:
   ```
   GEMINI_API_KEY=AIzaSyDbS-zqaSQWvAkFmYqfP5ef4gSHHzU9JcE
   ```
3. Закоммить и запушь:
   ```bash
   git add .env.production
   git commit -m "Добавлен .env.production с API ключом"
   git push
   ```
4. Railway пересоберет проект

⚠️ **ВНИМАНИЕ:** `.env.production` будет в репозитории, но это нормально для публичного проекта с API ключом клиента.

## Проверка:

После пересборки:
1. Открой бота в Telegram
2. Попробуй добавить транзакцию
3. Если работает - все ок!
4. Если нет - проверь логи выше

