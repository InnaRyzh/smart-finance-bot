# 🔍 Как проверить что данные сохраняются в Supabase

## Способ 1: Через веб-интерфейс Supabase (самый простой)

### Шаг 1: Открой Supabase
1. Зайди на [supabase.com](https://supabase.com)
2. Войди в свой аккаунт
3. Открой проект `smart-finance-bot`

### Шаг 2: Открой Table Editor
1. В левом меню нажми **"Table Editor"**
2. Выбери таблицу **`transactions`**

### Шаг 3: Проверь данные
1. В таблице должны быть все транзакции, которые ты добавил через бота
2. Каждая строка = одна транзакция
3. Колонки:
   - `id` - уникальный ID транзакции
   - `user_id` - твой Telegram ID (начинается с `tg_`)
   - `amount` - сумма
   - `category` - категория
   - `description` - описание
   - `date` - дата
   - `type` - INCOME или EXPENSE

### Шаг 4: Тест
1. Открой бота в Telegram
2. Добавь новую транзакцию: "Миша 500 гривен"
3. Вернись в Supabase Table Editor
4. Нажми кнопку **"Refresh"** (обновить)
5. Должна появиться новая строка с твоей транзакцией!

## Способ 2: Через логи сервера

### Проверь логи Railway:
1. Открой Railway → **Deployments** → **View Logs**
2. Ищи строки с `Supabase` или `saveTransaction`
3. Если видишь ошибки - значит проблема с подключением

## Способ 3: Через SQL Editor в Supabase

1. В Supabase открой **SQL Editor**
2. Выполни запрос:
```sql
SELECT * FROM transactions 
WHERE user_id = 'tg_ТВОЙ_TELEGRAM_ID'
ORDER BY date DESC;
```
3. Замени `tg_ТВОЙ_TELEGRAM_ID` на свой (можно посмотреть в Table Editor)

## Способ 4: Проверка через код (в консоли браузера)

1. Открой бота в Telegram
2. Открой DevTools (если возможно) или открой приложение в обычном браузере
3. В консоли выполни:
```javascript
// Проверка localStorage (fallback)
console.log('LocalStorage:', localStorage.getItem('smart_finance_transactions_v1'));

// Проверка Supabase (если настроен)
fetch('/api/check-supabase').then(r => r.json()).then(console.log);
```

## Что проверить:

✅ **Если данные есть в Supabase:**
- Значит все работает правильно!
- Данные синхронизируются между устройствами
- Есть резервное копирование в облаке

❌ **Если данных нет в Supabase:**
- Проверь переменные `SUPABASE_URL` и `SUPABASE_ANON_KEY` в Railway
- Проверь что таблица `transactions` создана в Supabase
- Проверь логи Railway на ошибки

## Важно:

- Данные сохраняются **И в Supabase И в localStorage** (гибридный режим)
- Если Supabase недоступен - используется localStorage
- Если Supabase доступен - данные синхронизируются

